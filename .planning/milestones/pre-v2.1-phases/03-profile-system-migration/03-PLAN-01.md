# Plan: Phase 03 - Profile System Migration & Polish

Upgrade the student identity and administrative control systems within the Nervalis terminal.

## Wave 1: SELF Tab & StudentCard Enhancements

### [1] Polish SELF Tab Stats
Modify `NervalisOverlay.tsx` to add animated status bars for Vitality and Neural Capacity.
<read_first>
- `apps/tma-client/src/features/dashboard/components/NervalisOverlay.tsx`
- `apps/tma-client/src/store/useTmaStore.ts`
</read_first>
<action>
In the `SELF` tab section of `NervalisOverlay.tsx`:
1. Add a **Vitality Bar** (Heart icon) mapping `status` to a 100% (ALIVE) or 0% (DEAD/MISSING) bar.
2. Add a **Neural Capacity Bar** (Zap icon) mapping `investigationPoints` (0-7) to a segmented bar.
3. If `isAssassin` is true, add a **Murder Points** display (Target icon) showing `murderPoints`.
4. Use Framer Motion for bar fill animations with a blue/cyan glow.
</action>
<acceptance_criteria>
- SELF tab displays two status bars.
- Bars animate when the tab is opened.
- Murder points only visible when `isAssassin` is true in the store.
</acceptance_criteria>

### [2] Update StudentCard Badges
Enhance `StudentCard` in `NervalisOverlay.tsx` to distinguish between PCs and NPCs.
<read_first>
- `apps/tma-client/src/features/dashboard/components/NervalisOverlay.tsx`
</read_first>
<action>
In the `StudentCard` component:
1. Add a small badge icon or text ("PC" or "NPC") based on `student.is_npc`.
2. PCs should have a blue accent, NPCs a subtle grey/zinc accent.
3. Update the `isOriginal` indicator to be more prominent.
</action>
<acceptance_criteria>
- Student cards in the PROFILES list show "PC" or "NPC" labels.
- The player's own card has a distinct "TU REGISTRO" marker.
</acceptance_criteria>

## Wave 2: PROFILES Search & Preview Expansion

### [3] Implement PROFILES Search
Add a search input to the PROFILES tab to filter the student list.
<read_first>
- `apps/tma-client/src/features/dashboard/components/NervalisOverlay.tsx`
</read_first>
<action>
1. Add a `searchQuery` state to `NervalisOverlay`.
2. Add a styled search input field above the student grid in the `PROFILES` tab.
3. Filter the `students` array by `tma_name` or `tma_title` using the search query.
4. Add a "No se encontraron registros" message if no students match.
</action>
<acceptance_criteria>
- Typing in the search box filters the student list in real-time.
- Searching by title (e.g., "Staff") correctly filters the list.
</acceptance_criteria>

### [4] Expand Student Preview Metadata
Display more character details in the selection preview of the PROFILES tab.
<read_first>
- `apps/tma-client/src/features/dashboard/components/NervalisOverlay.tsx`
- `apps/tma-client/src/features/characters/api.ts`
</read_first>
<action>
In the student selection preview block (PROFILES tab):
1. Access `student.tmc_character` data.
2. Display **Edad**, **Altura**, and **Peso** if available.
3. Add a "Ficha Técnica" header to the details section.
4. Improve the biography scroll area styling.
</action>
<acceptance_criteria>
- Selecting a student shows their extended metadata (Age/Height/Weight).
- Scrollbar in the biography section uses the `custom-scrollbar` class.
</acceptance_criteria>

## Wave 3: ADMIN Tab Possession Overhaul

### [5] Refactor Possession UI
Update the ADMIN tab's possession action to feel like a high-risk "Neural Overwrite".
<read_first>
- `apps/tma-client/src/features/dashboard/components/NervalisOverlay.tsx`
</read_first>
<action>
In the `ADMIN` tab's possession button section:
1. Change label from "Inyectar Posesión" to "EJECUTAR_SOBREESCRITURA_NEURAL".
2. Add a `confirming` state with a 3-second countdown or a "Confirm?" step.
3. Apply a red glitch effect and `animate-pulse` to the button when confirming.
4. Update the success toast to: `[OVERWRITE_SUCCESS]: Control total de ${student.tma_name} establecido.`
</action>
<acceptance_criteria>
- Possession requires a confirmation step.
- UI turns red/alert themed during the confirmation process.
- Possession still correctly updates the store via `setPossession`.
</acceptance_criteria>

## Verification Plan

### Automated
- `npm run build`

### Manual
1. Open Nervalis -> SELF: Verify bars fill correctly.
2. Open Nervalis -> PROFILES: Search for a specific student, verify filtering.
3. Open Nervalis -> PROFILES: Verify NPC/PC badges are visible.
4. Open Nervalis -> ADMIN (as Staff): Select a student, verify "Neural Overwrite" flow.
