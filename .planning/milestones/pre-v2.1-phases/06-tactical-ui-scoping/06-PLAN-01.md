# Plan: Phase 06 - Tactical UI Scoping

This plan addresses the refinement of UI visibility rules for the Assassination Poll and the Staff Identity Switcher to ensure immersive beta testing.

## Proposed Changes

### Store Helpers
#### [MODIFY] [useTmaStore.ts](file:///c:/Users/opbvn/Desktop/Wira/Programacion/TMC-The-research-for-hope/apps/tma-client/src/store/useTmaStore.ts)
- Add a selector helper or state flag for `isPossessing`.
- Logic: `state.myCharacterId !== state.originalCharacter?.id`.

### Poll Visibility
#### [MODIFY] [GlobalPollOverlay.tsx](file:///c:/Users/opbvn/Desktop/Wira/Programacion/TMC-The-research-for-hope/apps/tma-client/src/features/dashboard/components/GlobalPollOverlay.tsx)
- Update the condition at line 107.
- Change `userRole === 'roleplayer'` to a more inclusive check:
  - `(userRole === 'roleplayer' || (userRole === 'staff' && !isPossessing))`

### Switcher Scoping
#### [MODIFY] [GlobalTmaRegistry.tsx](file:///c:/Users/opbvn/Desktop/Wira/Programacion/TMC-The-research-for-hope/apps/tma-client/src/components/GlobalTmaRegistry.tsx)
- Use `usePathname()` to detect if the user is in a room.
- Hide `StaffIdentitySwitcher` if `pathname.startsWith('/rooms/')`.

## Verification Plan

### Automated Tests
- `npm run build` to ensure no regression.

### Manual Verification
1. **As Staff (Base)**:
   - Navigate to `/dashboard`. Verify Diamond Switcher is visible.
   - Navigate to `/rooms/lobby`. Verify Diamond Switcher is **hidden**.
   - Trigger an Assassin Poll (via DB/Admin). Verify Poll Overlay appears.
2. **As Staff (Possessing NPC)**:
   - Re-enter Dashboard. Flip to NPC.
   - Verify Poll Overlay **disappears** (if active).
3. **As Roleplayer**:
   - Verify Poll Overlay appears normally.
