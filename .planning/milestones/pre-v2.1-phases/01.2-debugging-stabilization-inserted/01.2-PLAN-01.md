# Phase 01.2: Debugging & Stabilization - Plan

Resolve accumulated UI inconsistencies, system-wide bugs, and QA improvements following the Nervalis 2.0 overhaul.

## Wave 1: Notification & Interaction Stabilization
Focuses on global state synchronization and draggable boundaries.

### Task 1: Notification Clearing Logic
- **Action**: Ensure `hasUnreadSignals` is reset to `false` in `useTmaStore` whenever the terminal is opened.
- **Files**:
  - `src/store/useTmaStore.ts`: Verify `toggleNervalis` implementation.
  - `src/features/dashboard/components/NervalisOverlay.tsx`: Add a safeguard `useEffect` to clear signals on mount if open.
- **Acceptance Criteria**: HUD signal disappears immediately upon terminal open.

### Task 2: Draggable Boundary Constraints
- **Action**: Apply `dragConstraints` and `dragElastic={0}` to the `motion.div` in `NervalisOverlay.tsx`.
- **Files**:
  - `src/features/dashboard/components/NervalisOverlay.tsx`
- **Acceptance Criteria**: Terminal cannot be dragged even partially off-screen.

### Task 3: Z-Index Hierarchy Audit
- **Action**: Audit and set explicit `z-index` values for the terminal vs. other overlays.
- **Target Hierarchy**:
  - `NervalisOverlay`: `z-[300]` (already set, keep it)
  - `PrivacyPollModal`: `z-[250]`
  - `StealthEntryDialog`: `z-[250]`
- **Files**:
  - `src/features/exploration/components/PrivacyPollModal.tsx`
  - `src/features/exploration/components/StealthEntryDialog.tsx`
- **Acceptance Criteria**: Terminal remains on top of all other dialogs.

## Wave 2: Investigation System Hardening
Focuses on voting logic and world-state synchronization.

### Task 4: Double-Voting Prevention
- **Action**: Implement `isVoting` local state in `PollCard` to block concurrent `submitVote` calls.
- **Files**:
  - `src/features/investigation/components/EvidenceTab.tsx`
- **Acceptance Criteria**: Buttons are disabled during the voting async request. Only one request sent per click.

### Task 5: Evidence Visibility Sync
- **Action**: Update `resolvePoll` to hide the evidence when accepted, and update `InsideRoomArena` to filter hidden clues.
- **Files**:
  - `src/features/investigation/api.ts`: Update `resolvePoll` to set `is_hidden: true`.
  - `src/features/exploration/components/InsideRoomArena.tsx`: Filter `clues` by `!c.is_hidden`.
- **Acceptance Criteria**: Evidence sprite disappears from 3D room immediately upon acceptance.

## Wave 3: Performance & Polish
Final aesthetic checks.

### Task 6: CRT Aesthetic Polish
- **Action**: Optimize CRT scanline opacity and mix-blend mode for better visibility without performance hit.
- **Files**:
  - `src/app/rooms/[roomId]/RoomPageClient.tsx`
- **Acceptance Criteria**: CRT effect is visible but doesn't obscure UI details.

## Verification Plan

### Automated Verification
- Run `npm run build` to ensure no type regressions.

### Manual Verification (UAT)
- **Boundary Test**: Drag terminal to screen edges.
- **Signal Test**: Trigger unread signal, open terminal, verify HUD icon clears.
- **Evidence Test**: Start poll, accept evidence, verify sprite vanishes from room.
- **Double Click Test**: Hammer the vote button; verify only one toast/request.
