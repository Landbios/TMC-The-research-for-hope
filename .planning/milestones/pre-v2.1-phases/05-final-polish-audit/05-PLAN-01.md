# Plan: Phase 05 - Final Polish & System Audit

Finalize the Nervalis 2.0 implementation with targeted bug fixes and system hardening.

## Wave 1: Movement & Possession Hardening

### [1] Audit Room Movement in AcademyMap
Ensure that room movement only affects the currently active character (`myCharacterId`).
<read_first>
- `apps/tma-client/src/features/exploration/components/AcademyMap.tsx`
</read_first>
<action>
1. Identify the room click handler.
2. Verify that the `updateCharacterRoom` API call uses the `myCharacterId` from the store.
3. Ensure no parallel updates are being fired for other possessed characters in the staff's session.
</action>
<acceptance_criteria>
- Staff movement only updates the active character's room in the database.
</acceptance_criteria>

## Wave 2: Logic & Stability Fixes

### [2] Fix Privacy Poll Modal Re-appearance
Ensure the modal closes permanently once resolved.
<read_first>
- `apps/tma-client/src/features/exploration/components/PrivacyPollModal.tsx`
- `apps/tma-client/src/store/useTmaStore.ts`
</read_first>
<action>
1. Add an explicit `setActivePrivacyPoll(null)` in the resolution handler.
2. Add a check in `InsideRoomArena` to ensure the modal isn't rendered if the status is not 'PENDING'.
</action>
<acceptance_criteria>
- Privacy Poll modal disappears immediately after resolution and does not flicker back.
</acceptance_criteria>

## Wave 3: Project Finalization

### [3] Requirements & State Update
Finalize all project documentation.
<action>
1. Move all completed requirements to 'Validated' in `PROJECT.md`.
2. Update `ROADMAP.md` to reflect full completion of Milestone 2.
3. Final build and lint check.
</action>
<acceptance_criteria>
- `npm run build` passes.
- Documentation is up to date.
</acceptance_criteria>

## Verification Plan

### Automated
- `npm run build`

### Manual
1. Test character switching + movement as staff.
2. Test privacy poll resolution.
